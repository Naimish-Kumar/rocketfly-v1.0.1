/// This class is used in the [categorieslist_item_widget] screen.
class CategorieslistItemModel {CategorieslistItemModel({this.all, this.isSelected, }) { all = all  ?? "🔥 All";isSelected = isSelected  ?? false; }

String? all;

bool? isSelected;

 }
