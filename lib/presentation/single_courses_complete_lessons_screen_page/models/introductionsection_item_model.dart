import '../../../core/app_export.dart';/// This class is used in the [introductionsection_item_widget] screen.
class IntroductionsectionItemModel {IntroductionsectionItemModel({this.groupBy, this.id, }) { groupBy = groupBy  ?? "";id = id  ?? ""; }

String? groupBy;

String? id;

 }
