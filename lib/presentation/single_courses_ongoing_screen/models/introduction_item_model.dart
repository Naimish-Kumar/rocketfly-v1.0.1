/// This class is used in the [introduction_item_widget] screen.
class IntroductionItemModel {IntroductionItemModel({this.groupBy, this.id, }) { groupBy = groupBy  ?? "";id = id  ?? ""; }

String? groupBy;

String? id;

 }
